# Документация: Управление Шаговыми Двигателями

## Краткий перечень функционала

- **Управление 4 шаговыми двигателями**:
  - Для каждого двигателя (1–4): кнопки «Вперёд», «Назад», «Стоп».
  - Настройка количества шагов (0–9999) через `QSpinBox`.
  - Настройка скорости (1–1000 мс/шаг) через `QSpinBox`.
- **Настройка COM-порта**:
  - Выбор порта из списка доступных (`QComboBox`).
  - Обновление списка портов кнопкой «Обновить порты».
  - Выбор скорости передачи (9600, 14400, 19200, 38400, 57600, 115200 baud).
  - Открытие/закрытие порта кнопкой «Открыть порт»/«Закрыть порт».
- **Мониторинг напряжения**:
  - Отображение текущего напряжения (в вольтах) в `QLabel` на основе данных от Arduino (`V:voltage`).
- **Консоль для логов**:
  - Сворачиваемая/разворачиваемая консоль (`QTextEdit`) для отображения сообщений (ошибки, отправленные команды, ответы Arduino).
  - Кнопка «Развернуть консоль»/«Свернуть консоль».
- **Индикация активного двигателя**:
  - Подсветка заголовка активного двигателя (`QGroupBox`) зелёным цветом (`#00B327`) при выполнении команды.
- **Настройки оформления**:
  - Выбор цвета элементов интерфейса (10 вариантов: черный, темно-серый, серый, светло-серый, темно-синий, голубой, темно-зеленый, зеленый, темно-красный, красный) через меню «Помощь → Выбрать цвет элементов».
  - Цвет применяется к кнопкам (`QPushButton`), границам `QSpinBox`, подчеркиванию `QComboBox` при фокусе, выделению меню, заголовкам и скроллбару в «О программе».
- **Сохранение настроек**:
  - Сохранение параметров (шаги, скорость для каждого двигателя, выбранный цвет) в `settings.json` при закрытии приложения или через меню «Файл → Сохранить настройки».

## Протокол обмена

Приложение взаимодействует с Arduino через serial-порт (по умолчанию 115200 baud). Протокол реализован для управления 4 шаговыми двигателями через три регистра сдвига SN74HC595 с защитой по напряжению через реле.

### Команды от приложения к Arduino

1. **Управление двигателем**:
   - Формат: `M# [F|B] steps speed_ms\n`
     - `M#`: Номер двигателя (`M1`, `M2`, `M3`, `M4`).
     - `[F|B]`: Направление (`F` — вперёд, `B` — назад).
     - `steps`: Количество шагов (целое, ≥0, до 9999 в приложении).
     - `speed_ms`: Скорость шага в миллисекундах (вещественное, >0, минимум 0.1 мс/шаг = 100 мкс/шаг).
   - Пример: `M1 F 100 100.0\n` (двигатель 1, вперёд, 100 шагов, 100 мс/шаг).
   - Реализация: Метод `send_motor_command` формирует команду из `QSpinBox`.

2. **Остановка всех двигателей**:
   - Формат: `STOP\n`
   - Действие: Останавливает все двигатели, отключает питание (регистры в `offByte`).
   - Пример: `STOP\n`
   - Реализация: Метод `send_stop_command` отправляет при нажатии «Стоп».

### Ответы от Arduino к приложению

1. **Напряжение**:
   - Формат: `V:voltage\n`
     - `voltage`: Напряжение (вещественное, в вольтах, 2 знака после запятой).
   - Частота: При запуске и каждые 1000 мс (если отладочный вывод закомментирован).
   - Пример: `V:11.57\n`
   - Реализация: `SerialThread` парсит `V:` и обновляет `voltage_label`.

2. **Отладочные сообщения**:
   - Формат: Текстовые строки с информацией или ошибками.
   - Примеры:
     - `Received: 'M1 F 100 100.0'` (подтверждение команды).
     - `Parsed: motor=1, dir_c=F, steps=100, speed_ms=100.000` (разбор команды).
     - `Starting: Motor 1 F 100 steps at 100000 us/step` (запуск двигателя).
     - `Completed: Motor 1 stopped. Sleep off.` (завершение работы).
     - `Relay: OFF (V > 10.4V, voltage=11.57V)` (отключение реле).
     - `Invalid command format: Expected 'M# [F|B] steps speed'` (ошибка формата).
   - Реализация: Строки, не начинающиеся с `V:`, отображаются в консоли через `data_received`.

## Подключение

### Пины Arduino
- **D8 (SER)**: Пин данных для первого SN74HC595 (pin 14, SER).
- **D9 (SRCLK)**: Пин тактирования для всех SN74HC595 (pin 11, SRCLK).
- **D10 (RCLK)**: Пин защёлки для всех SN74HC595 (pin 12, RCLK).
- **D2 (Relay)**: Управление реле (`HIGH` — включено, `LOW` — выключено).
- **A0 (Voltage)**: Аналоговый вход для делителя напряжения (R1=68kΩ, R2=47kΩ).

### Подключение сдвиговых регистров SN74HC595
Три регистра соединены последовательно:
- **Регистр 1 (Фазы M1, M2)**:
  - Q0–Q3: M1 (A+, B+, A-, B-).
  - Q4–Q7: M2 (A+, B-, A-, B+).
  - Подключение: Выходы к драйверу двигателя (например, ULN2003).
- **Регистр 2 (Фазы M3, M4)**:
  - Q0–Q3: M3 (A+, B+, A-, B-).
  - Q4–Q7: M4 (A+, B-, A-, B+).
  - Подключение: Выходы к драйверу двигателя.
- **Регистр 3 (Sleep)**:
  - Q0: Sleep M1.
  - Q1: Sleep M2.
  - Q2: Sleep M3.
  - Q3: Sleep M4.
  - Подключение: Выходы к сигналу Sleep/Enable драйвера.
- **Цепочка**:
  - SER (1-го регистра) → D8.
  - Q7' (1-го) → SER (2-го).
  - Q7' (2-го) → SER (3-го).
  - SRCLK (все) → D9.
  - RCLK (все) → D10.
  - VCC (pin 16) → 5V.
  - GND (pin 8) → GND.
  - OE (pin 13) → GND.
  - SRCLR (pin 10) → 5V.

### Делитель напряжения
- **A0**: Подключён к делителю (R1=68kΩ, R2=47kΩ).
- **R1**: Vin → A0.
- **R2**: A0 → GND.
- **Формула**: `Vin = ((analogRead(A0) * 5.0) / 1023) * ((68k + 47k) / 47k) * 1.037`.

### Реле
- **D2**: Управляющий вход реле (через транзистор/оптрон).
- **Контакты**: Питание двигателей через NO/COM.

## Особенности работы Arduino

- **Управление двигателями**:
  - Три SN74HC595:
    - Регистр 1: Фазы M1, M2.
    - Регистр 2: Фазы M3, M4.
    - Регистр 3: Sleep для M1–M4.
  - Пины: `dataPin=8`, `clockPin=9`, `latchPin=10`.
  - Фазы: A+, B+, A-, B- (последовательность в `phases_M#`).
  - Минимальная скорость: 0.1 мс/шаг (100 мкс).

- **Мониторинг напряжения**:
  - Пин A0, делитель (R1=68kΩ, R2=47kΩ).
  - Формула: `vInCalib = ((analogRead(A0) * 5.0) / 1023) * ((68k + 47k) / 47k) * 1.037`.
  - Частота: каждые 1000 мс.

- **Защита по напряжению**:
  - Реле на D2: `HIGH` (<10.0V), `LOW` (>10.4V).
  - Логика в `checkRelay`.

- **Ограничения**:
  - Команды: `M1`–`M4`, `F`/`B`, `steps` ≥ 0, `speed_ms` > 0.
  - Ошибки формата возвращают сообщение в serial-порт.
